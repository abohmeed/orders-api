name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pipeline not written yet
        run: echo "The CI pipeline will be generated in lesson 7.1"
